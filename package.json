{
  "name": "mingli-lab",
  "version": "0.5.0",
  "private": true,
  "description": "Tianji Bu - a black-and-white, local-first BaZi reading with plain-language reflections and traceable reasoning.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "chart": "tsx scripts/chart.ts",
    "format": "prettier --write src scripts tests vite.config.ts",
    "format:check": "prettier --check src scripts tests vite.config.ts"
  },
  "dependencies": {
    "lunar-typescript": "1.8.6"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prettier": "3.6.2",
    "tsx": "^4.20.6",
    "typescript": "~5.9.3",
    "vite": "8.3.0"
  }
}
